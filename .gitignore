# Windows
Thumbs.db
desktop.ini

# Mac
.DS_Store

# VS Code
.vscode/

# Secrets - never commit these
*.env
.env
secrets.*

# Local design previews - live in website/ so relative image paths work,
# but must never be committed (deploy.yml syncs all of ./website to S3)
website/preview-*.html
design-previews/
